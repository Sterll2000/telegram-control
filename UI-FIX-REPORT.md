# UI fix report

Исправлено:
- полностью убраны нативные `<select>` из интерфейса;
- добавлен единый тёмный кастомный выпадающий список в стиле Mini App;
- переработаны карточки раздела «Навигация» и элементы списков;
- улучшено отображение длинных описаний без некрасивого обрезания;
- улучшено окно просмотра материала;
- выбор роли и приоритета также переведён на новый выпадающий список;
- существующая логика списка админов и автоматизации Iris сохранена.

Проверки исходников:
- `<select>` в app/components: 0;
- `CustomSelect`: 4 использования;
- CSS для custom-select присутствует;
- API автоматизации админов присутствует;
- исходные TS/TSX-файлы присутствуют.

Полный `npm run typecheck` / `npm run build` в изолированной среде этого архива не выполнялся: установка npm-зависимостей из registry в этой среде завершилась по таймауту. На компьютере проекта после `npm install` рекомендуется выполнить `npm run typecheck` и `npm run build`.
